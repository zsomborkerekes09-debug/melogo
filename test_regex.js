const fs = require('fs'); const code = fs.readFileSync('C:/Users/zsomb/Documents/melogo_app/scratch/melogo/frontend/index.html', 'utf8'); const match = code.match(/<button[^>]*onclick="showScreen\('home-screen'\)"[^>]*>.*?Körülnézek.*?<\/button>/gis); if (match) { console.log('Matched ' + match.length + ' times. First match length: ' + match[0].length); console.log(match[0].substring(0, 100) + '...'); } else { console.log('No match'); }
